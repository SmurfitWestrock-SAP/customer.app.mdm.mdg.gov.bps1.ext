{
    "name": "app.mdm.mdg.gov.bps1.ext",
    "version": "0.1.0",
    "private": true,
    "description": "SWG Manage Business Partners - Central Governance",
    "keywords": [
        "ui5",
        "sapui5",
        "adaptation-project"
    ],
    "devDependencies": {
        "@sap/ux-ui5-tooling": "^1.19.1",
        "@ui5/cli": "^3.9.2",
        "@ui5/task-adaptation": "^1.3.0"
    },
    "scripts": {
        "build": "ui5 build --exclude-task generateFlexChangesBundle generateComponentPreload minify --clean-dest",
        "start": "fiori run --open /test/flp.html#app-preview",
        "start-editor": "fiori run --open /test/adaptation-editor.html",
        "deploy": "npm run build && fiori deploy --config ui5-deploy.yaml",
        "undeploy": "npm run build && fiori undeploy --config ui5-deploy.yaml",
        "déploy-test": "npm run build && fiori deploy --config ui5-deploy.yaml --testMode true"
    }
}
